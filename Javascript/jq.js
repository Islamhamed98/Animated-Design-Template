		/*
    [1] - document.getElementById()
    [2] - document.getElementsByTagName()
    [3] - document.getElementByClassName()
    [4] - document.querySelcetorAll()
    
    - document.title / document.images.src / document.forms.name.value /document.body
      document.anchors * document.links    
    
            var mydiv = document.getElementById('show');
               ==> put in div (the text of body )
                mydiv.innerText = document.body.innerText.indexOf('elZero');
                if (document.body.innerText.indexOf('elzero') > -1 )
                {
                    console.log(" Yes This is true ");
                }

    - innerHTML ==> to get html content 
    - TextContent ==> to get text content
    - Element.Attribute 
    - getAttribute("AttributeName") / setAttribute("AttributeName" , "AttrbuteValue" ) / if(myimg.hasAttribute)
    - ClassList - Length -  Item / myDiv.children - myDiv.childNode(kol 7aga in Div)
    - First_lastchild != First_lastELementChild / CreateELement - CreateTextNode/ appendChild(by7to f el25r)
    - InsertBefore (myNewElement , myMainDiv.childNodes[3]);
    - myMainDiv.removChild()
    - childNodes[0] - nodeValue - CloneNode() // to copy 
    - nextSibling - nextElementSibling
    - addEventListner / Contains / ClientHeight - ClientWidth //Area with padding
    - ScrollWidth - ScrollHeight area with scroll even it invisible area 
    - OffSetHeight(with everything but no margin(border-scroll-padding))
    - ScrollTop - ScrollLeft
    - ClientLeft / ClentTop (area with Border and Scroll)
    - Style ==> Element.Style.Property = value 
    - document.inputCoding (utf-8) / lastModified / URL The link of doc.
    - CreateELement / CreateComment / CreateAttribute / SetAttributeNode
     
*/









































